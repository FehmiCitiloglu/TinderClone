import {StyleSheetProperties} from 'react-native';
import {ColorType} from './value-types';

export type IconProps = {
  width?: number;
  height?: number;
  color?: ColorType;
  style?: StyleSheetProperties;
};
